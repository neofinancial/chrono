import { describe, expect, test } from 'vitest';

describe('ChronoMongoDatastoreTask', () => {
  describe('run', () => {
    test('should successfully run mongo task', async () => {
      await expect(true).toBeTruthy();
    });
  });
});
